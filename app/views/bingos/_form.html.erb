<%= form_for(bingo) do |f| %>
  <% if bingo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bingo.errors.count, "error") %> prohibited this bingo from being saved:</h2>

      <ul>
      <% bingo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
